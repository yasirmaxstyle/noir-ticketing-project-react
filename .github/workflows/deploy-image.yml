name: CI/CD - Build, Push & Deploy to Self-Hosted Server

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: self-hosted  # your remote server (runner) must be registered
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      # Log in to GitHub Container Registry (GHCR)
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build the Docker image and tag it as "latest" and with commit SHA
      - name: Build Docker image
        run: |
          docker build -t ghcr.io/yasirmaxstyle/noir:latest .
          docker tag ghcr.io/noir:latest ghcr.io/yasirmaxstyle/noir:${{ github.sha }}

      # Push both tags to GHCR
      - name: Push Docker image
        run: |
          docker push ghcr.io/yasirmaxstyle/noir:latest
          docker push ghcr.io/yasirmaxstyle/noir:${{ github.sha }}

      # Stop and remove old container (if running)
      - name: Stop old container (if any)
        run: |
          docker stop noir || true
          docker rm noir || true

      # Pull latest image
      - name: Pull latest image
        run: docker pull ghcr.io/yasirmaxstyle/noir:latest

      # Run new container
      - name: Run container
        run: |
          docker run -d --name noir -p 9502:9502 ghcr.io/yasirmaxstyle/noir:latest
